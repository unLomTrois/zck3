# This module will contain the logic for building a tabular LR automaton

The input will be a grammar, and the output will be a set of states and transitions between states

Main operations: CLOSURE(I) and GOTO(I, X)
